/*Elabore uma função para imprimir a seguinte cadeia de caracter na tela
A soma {a,b} =
A cadeia de caracter acima deve obrigatoriamente começar em uma nova
linha. Contudo, o cursor não deve pular de linha depois de imprimir a cadeia*/
#include <stdio.h>

void imprimir(){
  printf("A soma {a,b} =");
  return;
}

int main(void) {
  imprimir();
  return 0;
}